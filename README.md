# crop-prediction
🌱 Crop Yield Prediction is a machine learning project using Python, Pandas, NumPy, Matplotlib, Seaborn, and Scikit-learn. It covers data analysis, preprocessing, model training, and prediction. The trained model is integrated with Flask to provide crop yield predictions through a simple web application.
